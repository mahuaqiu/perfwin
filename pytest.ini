[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short

# 标记非 Windows 平台的测试为预期跳过
markers =
    windows_only: marks tests as requiring Windows (skip on non-Windows)